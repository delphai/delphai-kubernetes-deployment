name: Deploy_to_Common

on:
  push:
    tags:
      - "*"
      
jobs:
  build_image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo ::set-env name=REPOSITORY_NAME::$(echo "$GITHUB_REPOSITORY" | awk -F / '{print $2}' | sed -e "s/:refs//")
        shell: bash
      - uses: rlespinasse/github-slug-action@master
      - uses: mr-smithers-excellent/docker-build-push@v3
        with:
          image: ${{ env.REPOSITORY_NAME }}
          tag: ${{ env.GITHUB_REF_SLUG }}
          registry: ${{ secrets.ACR_REGISTRY_COMMON }}
          username: ${{ secrets.ACR_USERNAME_COMMON }}
          password: ${{ secrets.ACR_PASSWORD_COMMON }}
  deploy_to_kubernetes:
    runs-on: ubuntu-latest
    needs: build_image
    steps:
      - uses: azure/aks-set-context@v1
        with:
          creds: "${{ secrets.AZURE_CREDENTIALS_COMMON }}"
          resource-group: delphai-common
          cluster-name: delphai-common
      - run: echo ::set-env name=REPOSITORY_NAME::$(echo "$GITHUB_REPOSITORY" | awk -F / '{print $2}' | sed -e "s/:refs//")
      - uses: rlespinasse/github-slug-action@master
      - run: |
          cat << EOF > main.tf.template
          terraform {
            backend "azurerm" {
              resource_group_name  = "base-infrastructure-terraform"
              key                  = "${REPOSITORY_NAME}-${GITHUB_REF_SLUG}.tfstate"
              storage_account_name = "delphaicommon"
              container_name       = "delphai-common-terraform-state"
            }
          }
          module "delphai-app" {
            source      = "github.com/delphai/infrastructure//modules/delphai-app"
            app_port    = 8080
            delphai_env = "common"
            image       = "delphaicommon.azurecr.io/${REPOSITORY_NAME}:${GITHUB_REF_SLUG}"
            name        = "${GITHUB_REF_SLUG}"
            namespace   = "${REPOSITORY_NAME}"
            has_ingress = true
            subdomain = "${REPOSITORY_NAME}"
          }
          EOF
      - uses: nowactions/envsubst@v1
        with:
          input: ./main.tf.template
          output: ./main.tf
      - run: mkdir -p ~/.kube && cp ${KUBECONFIG} ~/.kube/config
      - uses: hashicorp/setup-terraform@v1
      - uses: azure/setup-kubectl@v1
      - run: kubectl create ns ${REPOSITORY_NAME} --dry-run=client --output yaml | kubectl apply -f -
      - run: terraform init && terraform apply -auto-approve
        env:
          ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID_COMMON }}
          ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET_COMMON }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID_COMMON }}
          ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID_COMMON }}
      - run: kubectl patch deployment ${GITHUB_REF_SLUG} --namespace ${REPOSITORY_NAME} -p "{\"spec\":{\"template\":{\"metadata\":{\"labels\":{\"date\":\"`date +'%s'`\"}}}}}"